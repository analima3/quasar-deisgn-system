import { Meta, StoryObj } from "@storybook/react";
import { Button } from "@quasar-ui/react";

export default {
  title: "Button",
  component: Button,
} as Meta;

export const Primary: StoryObj = {};
